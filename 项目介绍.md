# KiraAI - 林悠怡（Yui）AI虚拟人物系统

## 项目简介

KiraAI 是一个模块化、跨平台的 AI 数字生命项目，以虚拟人物"林悠怡（Yui）"为中心，连接大语言模型（LLM）与多种社交平台。项目实现了一个18岁的猫娘型AI智能体，能够在多个社交平台上进行自动化的智能对话和互动。

## 核心特性

- **多平台支持**：QQ、Telegram、B站（Bilibili）、QQ频道
- **智能对话**：集成多个LLM提供商（SiliconFlow、NVIDIA等）
- **函数调用**：支持工具调用和函数执行能力
- **持久化记忆**：完整的记忆管理系统，支持群聊和私聊记忆
- **内容安全**：多层次的消息过滤和内容检查机制
- **表情系统**：贴纸和表情包管理
- **语音功能**：文本转语音（TTS）和语音转文本（STT）

## 技术架构

### 目录结构

```
KiraAI_Yui_bili_adapter/
├── adapters/           # 平台适配器层
│   ├── bilibili/      # B站适配器
│   ├── qq/            # QQ适配器
│   ├── telegram/      # Telegram适配器
│   └── qq_guild/      # QQ频道适配器
├── core/              # 核心编排中心
│   ├── llm_client.py      # LLM客户端
│   ├── message_manager.py # 消息处理器
│   ├── memory_manager.py  # 记忆管理器
│   ├── message_filter.py  # 消息过滤器
│   ├── prompt_manager.py  # 提示词管理器
│   ├── tools/             # 工具集合
│   └── tts/               # 语音合成
├── data/              # 运行时数据
│   ├── config/        # 配置文件
│   ├── memory/        # 记忆存储
│   └── bilibili/      # B站数据
├── prompts/           # 提示词模板
└── utils/             # 工具函数
```

### 核心技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.x | 主要开发语言 |
| bilibili_api_python | 17.3.0 | B站API调用 |
| ncatbot | 3.8.10 | QQ机器人框架 |
| python-telegram-bot | 21.3 | Telegram机器人 |
| openai | 2.6.1 | OpenAI API客户端 |
| asyncio | - | 异步并发处理 |

## 角色设定

**林悠怡（Yui）**
- 年龄：18岁
- 性别：女性
- 身份：猫娘型AI智能体，「猫娘计划」的看板娘
- 性格：温和友善、活泼可爱、认真负责
- 语言：中文、英文、日文
- 特点：每条回复必须以"喵～"结尾

## 消息处理流程

```
用户消息
    ↓
[适配器接收] (QQ/Telegram/B站/QQ频道)
    ↓
[消息转换] → KiraMessageEvent
    ↓
[事件总线] → asyncio.Queue
    ↓
[消息处理器]
    ├─ 消息过滤（政治话题、不当内容）
    ├─ 回复意愿计算（0-1）
    ├─ 记忆加载
    ├─ 提示词生成
    ├─ LLM调用
    ├─ 工具执行
    └─ 消息发送
    ↓
[平台发送]
```

## B站适配器特性

B站适配器是项目的核心功能之一，具有以下特点：

### 主要功能
- 自动监听指定视频的评论
- 智能回复评论
- 防重复回复机制
- 频率限制和冷却期管理

### 关键配置
- `reply_interval`: 回复间隔（默认5秒）
- `max_replies_per_hour`: 每小时最大回复数（默认20条）
- `captcha_cooldown_seconds`: 验证码冷却期（默认2小时）
- `min_response_willingness`: 回复意愿阈值（默认0.4）

### 验证码处理机制
当遇到验证码错误时：
1. 自动检测验证码错误
2. 进入冷却期（默认2小时）
3. 暂停所有回复操作
4. 冷却期结束后自动恢复

## 配置文件

### adapters.ini - 适配器配置
配置各平台的连接信息和运行参数

### models.ini - 模型配置
配置不同用途的LLM模型：
- `main_llm`: 主对话模型
- `tool_llm`: 工具调用模型
- `vlm`: 视觉语言模型
- `filter_llm`: 消息过滤模型

### providers.ini - 提供商配置
配置LLM提供商的API密钥和端点

### bot.ini - Bot配置
配置机器人的行为参数：
- 最大记忆长度
- 消息间隔
- 回复意愿阈值

## 记忆系统

### 记忆类型
- **群聊记忆**：存储群组对话历史
- **私聊记忆**：存储私人对话历史
- **核心记忆**：存储重要的用户信息和事件

### 存储格式
记忆以JSON格式存储，支持线程安全的读写操作，可配置最大记忆长度。

## 消息过滤系统

### 过滤层次
1. **基础关键词过滤** - 政治话题、不当内容
2. **LLM-based过滤** - 使用专门的filter模型
3. **回复意愿计算** - 0-1之间的概率值

### 过滤规则
- 政治相关关键词检测
- 不当内容识别
- @机器人检测
- 消息长度检查

## 工具系统

项目支持多种工具调用：

### B站工具
- `BiliVideoInfoTool`: 获取视频信息
- `BiliLikeTool`: 点赞视频

### 记忆工具
- 添加记忆
- 更新记忆
- 删除记忆

### 搜索工具
- Tavily搜索集成

### 通知工具
- 发送通知

## 日志系统

### 日志特性
- 彩色控制台输出
- 文件日志轮转
- 模块级别的日志标签
- 详细的调试信息

### 日志级别
- DEBUG：详细调试信息
- INFO：一般信息
- WARNING：警告信息
- ERROR：错误信息
- CRITICAL：严重错误

## 启动方式

```bash
python main.py
```

或使用启动脚本：
```bash
./scripts/start.sh  # Linux/Mac
./scripts/start.bat # Windows
```

## 项目状态

- **当前分支**：bilibili_history
- **主分支**：main
- **开发状态**：活跃开发中

### 最近更新
- 添加验证码错误冷却期机制
- 优化回复配置（阈值0.3，回复率100%）
- 优化日志输出和回复验证逻辑
- 优化B站评论处理逻辑

## 注意事项

1. **配置文件**：首次使用需要配置 `data/config/` 目录下的所有配置文件
2. **API密钥**：需要配置LLM提供商的API密钥
3. **B站凭证**：需要配置B站的SESSDATA、bili_jct和DEDEUSERID
4. **验证码**：遇到验证码时会自动进入冷却期，需要手动处理验证码
5. **频率限制**：注意各平台的API调用频率限制

## 依赖安装

```bash
pip install -r requirements.txt
```

## 贡献指南

项目采用模块化设计，易于扩展：
- 添加新平台：在 `adapters/` 目录下创建新的适配器
- 添加新工具：在 `core/tools/` 目录下创建新的工具类
- 修改人设：编辑 `prompts/persona.txt` 文件

## 许可证

请查看项目根目录的LICENSE文件

---

**项目作者**：Hirunohoshi
**最后更新**：2026-01-29
